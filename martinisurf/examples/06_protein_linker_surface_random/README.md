# Protein Linker + Random Surface Linkers Example

Builds a protein-on-surface system in linker mode and adds extra random linkers on the surface.

Run:
```bash
bash run.sh
```

Notes:
- Uses `--go --dssp` explicitly for protein martinization.
- `--surface-linkers 4` adds 4 extra random linker copies on the surface.
